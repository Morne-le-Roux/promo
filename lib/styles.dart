import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';

// This is the place to edit all of the const styles etc.
// Font sizes should be declared in the widget itself with .copywith(), as the multiplier for screen width must be in the context of the page.

final TextStyle kMainHeadingTextStyle =
    GoogleFonts.russoOne(color: Colors.white);

final TextStyle kSecondaryHeadingTextStyle =
    GoogleFonts.righteous(color: Colors.white);

const Color kBackgroundColor = Color.fromARGB(255, 6, 13, 22);
