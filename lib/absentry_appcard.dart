import 'package:flutter/material.dart';

class AbSentryAppCard extends StatelessWidget {
  const AbSentryAppCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding: EdgeInsets.all(20),
      child: Placeholder(),
    );
  }
}
